<%= form_for [:admin, :polygon, @document], :html => {:multipart => true} do |f| %>
  <div class="row">
  <% if @document.errors.any? %>
    <div class="alert alert-danger">
      <strong><%= pluralize(@document.errors.count, "Error") %></strong> prohibieron a este Documento guardar la información!
      <ul>
        <% @document.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>
  <%= f.hidden_field :polygon_id %>
  <div class="form-group">
    <%= f.label :name, "Nombre" %>
    <%= f.text_field :name, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :file, "Archivo" %>
    <%= f.file_field :file, :class => "form-control" %>
    <%= @document.file.file.filename if @document.file.present? %>
  </div>

  <div class="form-group">
    <%= f.label :visoble, "Visible a" %>
    <%= f.select :visible, ['Publico', 'Capacitador'], {:class => "form-control"} %>
  </div>
  <p><%= f.submit  "Guardar", :class => 'btn btn-success' %></p>
<% end %>
